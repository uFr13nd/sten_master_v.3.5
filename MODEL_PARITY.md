# Соответствие расчётной модели Google Sheets v3.5 и Web

Документ фиксирует формулы, которые реализованы в `engine.js`.

## Общие входные данные

- город объекта;
- `Sпрод` — продаваемая площадь;
- `Sобщ` — общая площадь;
- `Pнар` — периметр наружных стен;
- `Pмеж` — периметр межквартирных стен;
- `Pпер` — периметр перегородок;
- `H` — высота этажа;
- `N` — этажность;
- `Cпрод` — стоимость 1 м² продаваемой площади.

## Эффективная толщина

```text
если отделка = «Без отделки»:
    Tэфф = Tкладки
иначе:
    Tэфф = Tс_отделкой
```

Точно так же определяется толщина проектного решения.

---

# Наружные стены

## Площадь стены по основанию

```text
Sосн = Pнар × Tэфф / 1000
```

## Объём кладки на этаж

```text
Vэтаж = Pнар × Tкладки / 1000 × H
```

## Количество условных единиц

```text
Q = Vэтаж / (L × W × h) × 1.07
```

## Масса на этаж

```text
Mэтаж = Vэтаж × плотность / 1000
```

## Стоимость работы + материалы

```text
Cэтаж = Vэтаж × ЦенаКонструкции
Cобъект = Cэтаж × N
```

`ЦенаКонструкции`, `Работы/м³` и `Материалы/м³` приходят из рассчитанной `База_Наружные`.

## Машины

```text
по_массе  = ROUNDUP(Mэтаж × N × 1000 / грузоподъёмность_кг)
по_объёму = ROUNDUP(Vэтаж × N / объём_кузова)
машины = IF(Vэтаж <= 0, 0, MAX(по_массе, по_объёму, 1))
```

## Доставка

```text
Cдоставка = машины × расстояние × ставка_руб_км
```

## Внутренняя отделка наружной стены

Проёмы: 15%.

```text
Sотделки_этаж = Pнар × H × (1 - 0.15)
Cотделки = ставка_отделки × Sотделки_этаж / 2 × N
```

Деление на 2 сохранено намеренно: `База_Отделка` содержит двухстороннюю ставку, а у наружной стены считается внутренняя сторона.

## Итого

```text
ИТОГО = Cобъект + Cдоставка + Cотделки
```

---

# Межквартирные стены

Геометрия, объём, масса, цена и доставка считаются аналогично наружным стенам.

Проёмы для отделки: 7%.

```text
Sотделки_этаж = Pмеж × H × (1 - 0.07)
Cотделки = Sотделки_этаж × N × ставка_отделки
```

Двухсторонность уже заложена в ставке `База_Отделка`.

---

# Перегородки

## Площадь кладки на этаж

```text
Sкладки = Pпер × H × (1 - 0.10)
```

## Площадь по основанию

```text
Sосн = Pпер × Tэфф / 1000
```

## Количество условных единиц

```text
Q = Sкладки / (L × h) × 1.07
```

## Масса

```text
Mэтаж = Sкладки × Tкладки / 1000 × плотность / 1000
```

## Цена

```text
Cэтаж = Sкладки × ЦенаКонструкции_за_м²
Cобъект = Cэтаж × N
```

## Объём доставки

Для кладочных перегородок:

```text
Vдост_этаж = Sкладки × Tкладки / 1000
```

Для каркасной ГВЛ:

```text
Vдост_этаж = Sкладки × 4 × (2.5 × 1.2 × 0.01) + Sкладки × 0.05
```

Для другой каркасной перегородки (ГКЛ):

```text
Vдост_этаж = Sкладки × 4 × (2.5 × 1.2 × 0.0125) + Sкладки × 0.05
```

Машины на объект:

```text
MAX(
  ROUNDUP(Mэтаж × N × 1000 / грузоподъёмность),
  ROUNDUP(Vдост_этаж × N / объём_кузова),
  1
)
```

при нулевой площади — 0.

## Отделка

```text
Sотделки_этаж = Sкладки
Cотделки = Sотделки_этаж × N × ставка_отделки
```

---

# Ставки отделки

Точная ветка главного листа:

```text
Без отделки → 0

если материал содержит «Каркасная»:
    Предчистовая → ГВЛВ / Предчистовая
    Чистовая     → ГВЛВ / Чистовая
иначе:
    Предчистовая → Кирпич/блок / Предчистовая
    Чистовая     → Кирпич/блок / Чистовая
```

Сами ставки берутся из рассчитанной `База_Отделка`, а не дублируются вручную в браузере.

---

# Итоговая сводка относительно проекта

Для каждой категории отдельно:

```text
если проектный материал пустой → 0
если материал сценария = проектному материалу → 0
иначе:
    ΔSстен = периметр × (Tэфф_сценария - Tэфф_проекта) / 1000
```

Общий показатель:

```text
ΔSстен_этаж = сумма трёх категорий
ΔSпрод_этаж = -ΔSстен_этаж
ΔSпрод_объект = ΔSпрод_этаж × N
ΔСтоимостьПродаваемой = ΔSпрод_объект × Cпрод
```

## Стоимость изменяемого решения

Для категории:

```text
если материал сценария = проектному материалу:
    СтоимостьИзменения = 0
иначе:
    СтоимостьИзменения = полный ИТОГО категории
```

Тип отделки при совпадении материала **не меняет это правило**, поскольку именно так работает Google Sheets v3.5.

```text
ИТОГО_ИЗМЕНЯЕМЫХ = наружные + межквартирные + перегородки
Цена/м² общей = ИТОГО_ИЗМЕНЯЕМЫХ / Sобщ
Цена/м² продаваемой = ИТОГО_ИЗМЕНЯЕМЫХ / Sпрод
```

---

# Дополнительный KPI веб-интерфейса

Веб-интерфейс показывает отдельно помеченный аналитический показатель:

```text
Net-эффект = ΔСтоимостьПродаваемой - ИТОГО_ИЗМЕНЯЕМЫХ
```

Он **не заменяет и не изменяет** формулы Google Sheets, а является дополнительной визуальной метрикой.

---

# Расценки и состав конструкций

Веб-приложение не пытается независимо воспроизводить сотни внутренних формул строк `База_*` после изменения прайса.

При изменении цены через Web API:

1. меняется централизованный `W-xxx` или `M-xxx` в Google Sheets;
2. Sheets пересчитывает собственные формулы баз;
3. Web API считывает новые рассчитанные значения `Цена конструкции / Работы / Материалы`;
4. браузер получает уже пересчитанную базу.

Это гарантирует единый источник сметной истины и устраняет риск того, что JavaScript и Sheets по-разному округлят или трактуют редкую строку состава.

---

# Golden tests

Тест `tests/golden.test.js` проверяет 186 значений текущего эталонного сценария Google Sheets.

На момент сборки:

```text
186 / 186 PASS
```
